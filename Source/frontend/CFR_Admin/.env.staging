# NOTE (production-readiness review): no dedicated, isolated staging backend has been
# provisioned yet. This intentionally points at the same origin as .env.live until a real
# staging API exists — do NOT treat a `staging`-mode build as a safe, isolated test
# environment; every request it makes hits the live database. src/app/config/gateway.ts
# logs a loud console warning at startup when it detects this exact condition, as a runtime
# guard against mistaking this for isolated staging. Replace this URL the moment a real
# staging backend is provisioned.
VITE_APP_REST_API_BASE_URL=https://cfrapi.allnewoptionc.com
